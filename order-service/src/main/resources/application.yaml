spring:
  application:
    name: order-service
  config:
    import:
      - nacos:order-service.yaml
      - nacos:kafka.yaml
      - nacos:redis.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.186:8848
      config:
        server-addr: 192.168.0.186:8848
        file-extension: yaml
        group: DEFAULT_GROUP
